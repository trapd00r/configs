###< vim: set ft=zsh:fdm=marker:fmr=#<,#>:fdl=0:fen:et:sw=2:fcl=all:
###
#      ${HOME}/etc/zsh/02-bookmarks.zsh
#   ‗‗‗‗‗‗‗‗‗‗‗‗ ‗‗‗‗‗‗ ‗‗‗‗‗‗‗‗ ‗‗‗‗‗‗‗‗‗‗‗
#         owner  Magnus Woldrich <magnus.woldrich@gmail.com>
#         btime  2009-04-24
#         mtime  2016-09-05 14:56:04
#   permissions  You are free to use things you may find useful here.
#                Would my tweaks happen to give you a raise or fetch you a
#                girlfriend, it goes without saying I'm counting on you
#                to share with me as I've shared with you.
#           git  http://github.com/trapd00r/configs/zsh  (up-to-date)
#           url  http://devel.japh.se/configs/zsh        (sync 1/24h)
#           irc  japh@freenode #zsh #vim #perl
#   ‗‗‗‗‗‗‗‗‗‗‗‗ ‗‗‗‗‗‗‗‗‗‗‗‗‗ ‗‗‗‗ ‗‗‗‗ ‗‗‗‗
###
###>

#hash -d '♫'=${HOME}/♫
#hash -d '⚐'=${HOME}/⚐

#hash -d "♫"=${HOME}/.CD-current

hash -d     b=${HOME}/bin
hash -d     c=${HOME}/etc
hash -d     d=${HOME}/dev
#hash -d     h=${HOME}/http
#hash -d     u=${HOME}/dev/utils
#hash -d     v=${HOME}/etc/vim
#hash -d     x=${HOME}/etc
#hash -d    dD=${HOME}/dev/Distributions
#hash -d    rc=${HOME}/etc/rc.d/
#hash -d    up=${HOME}/dev/_upstream
#hash -d   dLS=${HOME}/dev/LS_COLORS
#hash -d   dTE=${HOME}/dev/Term-ExtendedColor
#hash -d  "ｑ"=${HOME}/dev/Term-ExtendedColor
#hash -d  dTET=${HOME}/dev/Term-ExtendedColor-TTY
#hash -d  dTEX=${HOME}/dev/Term-ExtendedColor-Xresources
#hash -d  dfls=${HOME}/dev/File-LsColor
#hash -d  drpd=${HOME}/dev/RPD
