rpws init 4 -k
escape C-f
startup_message off

bind h focusleft
bind j focusdown
bind k focusup
bind l focusright
bind H rpwsp
bind L rpwsn
bind c exec urxvtc -name URxvt.india
bind e exec sh dmenushit

